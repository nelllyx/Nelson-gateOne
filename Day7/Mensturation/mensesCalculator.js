const prompt = require("prompt-sync")();

let cycleLength = parseInt(prompt("How long is your cycle(in days): "))

let startDate = prompt("Enter the start date of your last period?( in dd-mm-YYYY date format ) ")


console.log(cycleLength)